apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: image-server-claim0
  name: image-server-claim0
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 100Mi
