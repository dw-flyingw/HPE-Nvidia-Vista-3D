# ============================================================================
# Ubuntu Server Deployment - Environment Configuration
# ============================================================================
# Copy this file to .env and adjust settings for your Ubuntu server
# ============================================================================

# =============================================================================
# SCENARIO 1: Frontend and Backend on the SAME Ubuntu server
# =============================================================================
# Use the container names for communication (recommended, fastest)

VISTA3D_SERVER="http://vista3d-server:8000"
VISTA3D_IMAGE_SERVER_URL="http://image-server:8888"
IMAGE_SERVER="http://localhost:8888"
EXTERNAL_IMAGE_SERVER="http://localhost:8888"

# Data directories (adjust to your paths)
DICOM_FOLDER="/home/user/data/dicom"
OUTPUT_FOLDER="/home/user/data/output"

# Docker
COMPOSE_PROJECT_NAME="vista3d-platform"

# Backend NGC credentials (required for Vista3D)
NGC_API_KEY="nvapi-YOUR_API_KEY_HERE"
NGC_ORG_ID=""

# =============================================================================
# SCENARIO 2: Frontend and Backend on DIFFERENT Ubuntu servers
# =============================================================================
# Replace 192.168.1.100 with your backend server's IP address
# Uncomment these if frontend is on a different machine

# VISTA3D_SERVER="http://192.168.1.100:8000"
# VISTA3D_IMAGE_SERVER_URL="http://192.168.1.100:8888"

# Note: Ensure firewall allows ports 8000 and 8888:
#   sudo ufw allow 8000/tcp
#   sudo ufw allow 8888/tcp
#   sudo ufw enable

# =============================================================================
# SCENARIO 3: Mac development with Ubuntu backend (via SSH tunnel)
# =============================================================================
# Uncomment these if developing from Mac using SSH tunnels to Ubuntu

# # From Mac terminal, establish tunnels:
# # ssh -L 8000:localhost:8000 -R 8888:localhost:8888 user@ubuntu-ip

# VISTA3D_SERVER="http://host.docker.internal:8000"
# VISTA3D_IMAGE_SERVER_URL="http://host.docker.internal:8888"
# IMAGE_SERVER="http://localhost:8888"

# ============================================================================
# Quick Start Commands
# ============================================================================
#
# 1. On Ubuntu backend (run these in the backend directory):
#    cd ~/path/to/backend
#    docker-compose -f docker-compose.yml up -d
#
# 2. On Ubuntu frontend (run these in the frontend directory):
#    cd ~/path/to/frontend
#    docker-compose -f docker-compose.yml up -d
#
# 3. Access frontend:
#    http://localhost:8501
#
# ============================================================================
