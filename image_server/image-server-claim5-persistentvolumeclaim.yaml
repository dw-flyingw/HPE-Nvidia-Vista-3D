apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: image-server-claim5
  name: image-server-claim5
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
